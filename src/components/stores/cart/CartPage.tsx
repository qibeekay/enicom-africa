import { CartDetails, DasboardNav } from '@/components';
import React from 'react';

const CartPage = () => {
	return (
		<div>
			<DasboardNav />
			<CartDetails />
		</div>
	);
};

export default CartPage;
