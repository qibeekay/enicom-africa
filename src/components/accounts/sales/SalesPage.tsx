import React from 'react';
import { AccountNav, SalesMain } from '@/components';
const SalesPage = () => {
	return (
		<div>
			<AccountNav />
			<SalesMain />
		</div>
	);
};

export default SalesPage;
