import { AccountMain, AccountNav } from '@/components';
import React from 'react';

const AccountPage = () => {
	return (
		<div>
			<AccountNav />
			<AccountMain />
		</div>
	);
};

export default AccountPage;
